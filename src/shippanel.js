import GridPanel from "./gridpanel";
export default function ShipPanel(title){
  const HTML = GridPanel(title);
  
  function renderOnBoard(shipType, coordinate){
    //shows ships on board    
  }

  function renderHit(){
    //changes ships tile colour
  }

  function remove(){
    //clears tiles
  }

}


